spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: customer_service_chatbot_dev

app:
  chat:
    storage:
      mode: mongo
  websocket:
    allowed-origins:
      - http://localhost:4200
      - http://localhost:8080


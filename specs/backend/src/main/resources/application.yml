spring:
  application:
    name: customer-service-backend
  profiles:
    default: local

app:
  security:
    jwt:
      issuer: customer-service-backend
      expiration-minutes: 60

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
